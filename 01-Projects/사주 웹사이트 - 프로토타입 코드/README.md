---
tags: [부수입, 사주, 웹사이트, 프로토타입, 코드]
created: 2026-08-04
---

# 사주 웹사이트 프로토타입 코드

DB를 코드에서 분리해 JSON 파일로 관리하는 버전입니다.

> **2026-08-04 업데이트**: 27명 → 65명(60갑자 100% 커버) → 121명 → 116명(정치인 4명·이재용 제거) → **161명**(전 갑자 최소 2명)으로 순차 확장/정리.

## 파일 구성
- `celebrities.json` — 유명인 161명 데이터 (이름, 일주, 성공스토리, 습관, 모토) — 60갑자 전체 커버, 갑자당 2~3명
- `ohang_copy.json` — 오행(木火土金水) 5개 설명 카피
- `match.py` — 매칭 로직 (일주 정확 일치 → 없으면 오행 기준 폴백, 생년월일 해시 기반 고정 시드 적용). Python/서버용 참고 구현
- `index.html` — **프론트엔드 프로토타입** (입력폼 + 결과카드). `celebrities.json`/`ohang_copy.json`을 fetch로 불러옴 → **로컬 서버로 열어야 동작함** (아래 실행 방법 참고)
- `privacy-policy.html` / `terms.html` — 개인정보처리방침 / 이용약관 (애드센스 신청 필수 요건, 초안)
- `og-image.png` — 링크 공유 시 미리보기 이미지 (1200×630, Canvas로 생성)
- `sitemap.xml` — 검색엔진 제출용 사이트맵 (`sajutwin.com` 도메인 반영 완료)
- `celebs/` — **유명인별 개별 페이지 161개 + 목록 페이지** (SEO용, 아래 설명)

## 실행 방법
```bash
# 백엔드 로직만 확인
pip install lunar_python
python3 match.py

# 프론트엔드 프로토타입 (반드시 로컬 서버로 열 것 — file://로 직접 열면 fetch가 막힘)
python3 -m http.server 8000
# 브라우저에서 http://localhost:8000/index.html 접속
```

## index.html 동작 방식
- 생년월일을 입력하면 JS에서 바로 일주(日柱)를 계산 (서버/API 없이 클라이언트에서 완결)
- **일주 계산 방식**: 율리우스일(JDN)을 구해 `(JDN + 49) mod 60`으로 60갑자 인덱스 계산. 이 offset(49)은 `match.py`(lunar_python 기준)의 계산 결과 28개 샘플 날짜(1769~1993년)와 전수 대조해서 검증됨 — 일주는 절기(월 경계)와 무관하게 날짜만으로 정해지는 60일 주기라, 복잡한 만세력 라이브러리 없이도 JS 몇 줄로 정확히 계산 가능
- 정확 일치 유명인이 없으면 오행 기준으로 폴백하되, FNV-1a 해시 + mulberry32 PRNG로 시드를 고정해 같은 날짜는 항상 같은 결과가 나오도록 함 (`match.py`의 Python 시드 로직과 동일한 목적)
- **DB는 `celebrities.json`/`ohang_copy.json`에서 `fetch()`로 불러옴** (더 이상 `index.html`에 데이터가 중복 저장되지 않음). 로딩 중엔 버튼이 비활성화(`데이터 불러오는 중...`)되고, `file://`로 직접 열어 fetch가 차단되면 로컬 서버 실행법을 안내하는 에러 메시지가 표시됨
- **결과 카드 이미지 저장/공유**: "카드 이미지로 저장/공유" 버튼 클릭 시 `<canvas>`로 결과 카드를 직접 그려 PNG로 만듦 (html2canvas 등 외부 라이브러리 불필요). 오행별 포인트 컬러(`OHANG_COLORS`)를 적용한 그라디언트 배경에 일주 배지·오행 타이틀·키워드·명언·매칭된 유명인(최대 2명)을 그려넣음
  - **2-패스 렌더링**: 임시 캔버스로 모든 텍스트를 먼저 측정해 줄바꿈 라인 수를 계산하고, 그 값으로 카드 전체 높이를 정확히 산출한 뒤 실제 캔버스에 그림 — 유명인 모토 길이에 따라 카드 높이가 자동으로 늘어나며, 하단 텍스트가 겹치는 문제 없음
  - 생성된 이미지는 모달로 미리보기, `<a download>`로 즉시 다운로드 가능
  - `navigator.share`(Web Share API, 파일 공유 지원 시)가 있으면 "공유하기" 버튼도 노출 → 모바일에서 카카오톡/인스타그램 등으로 바로 공유 가능. 미지원 브라우저(대부분의 데스크톱)에서는 다운로드만 제공
- **Open Graph / 트위터 카드 메타태그** 추가 완료 — 카카오톡/페이스북/X 등에 링크 공유 시 `og-image.png`가 미리보기로 노출됨 (도메인 확정 후 `og:url`을 상대경로가 아닌 절대 URL로 교체 권장)

## celebs/ — 유명인별 개별 페이지 (SEO)
- `celebs/<유명인이름>.html` 161개 — 각 인물의 사주(일주)·성공스토리·습관·모토를 담은 정적 페이지. "OOO 사주" 같은 이름 기반 검색 유입을 노리기 위해 생성
- `celebs/index.html` — 오행별로 정리된 전체 유명인 목록 페이지 (161명, 木/火/土/金/水 각각 29~35명)
- `gen_celeb_pages.py` — `celebrities.json`을 읽어 위 페이지들과 `sitemap.xml`을 생성하는 스크립트. **DB(`celebrities.json`)가 바뀌면 `python3 gen_celeb_pages.py`를 다시 실행해 페이지를 재생성해야 함** (수동으로 161개를 고칠 필요 없음)

## 검증 (Playwright 헤드리스 브라우저로 실제 확인)
- 입력폼 렌더링 정상, JS 콘솔 에러 없음
- 1955-02-24(스티브 잡스 생일) 입력 → 정확 매칭으로 **스티브 잡스 + 레오나르도 디카프리오** 2명 동시 노출 확인
- 1990-03-15 입력 → 정확 매칭으로 **비(정지훈)·박명수·칼 라거펠트** 3명 동시 노출 확인 (161명 확장 후 갑자당 최대 3명까지 매칭 가능)
- 161명 DB 기준 60갑자 100% 커버 확인 (`Object.keys(ILJU_INDEX).length === 60`), 이름 중복 없음
- 카드 이미지는 매칭 인원이 3명이어도 대표 2명만 그려 카드 가독성 유지 (`r.celebrities.slice(0, 2)`), 나머지는 페이지 결과 목록에서 전체 확인 가능
- Node.js로 `index.html`의 로직 부분만 추출해 Python(`match.py`) 계산 결과와 다수 날짜 교차검증 → 일치
- **카드 이미지 저장 기능**: "카드 이미지로 저장/공유" 클릭 → 모달에 카드 이미지 정상 표시, 다운로드 링크 동작 확인. 캔버스에서 생성된 PNG를 직접 추출해 실제 렌더링 결과(오행 배지·타이틀·키워드·명언·유명인 2명)를 시각적으로 확인
  - 최초 구현 시 유명인 박스 높이를 고정값으로 계산해 하단 "매칭 유형" 텍스트가 겹치는 버그 발견 → 2-패스(측정 후 그리기) 방식으로 재작성해 해결
  - "닫기" 버튼이 투명 배경이라 뒤 배경 텍스트가 비쳐 보이는 문제도 발견해 불투명 배경으로 수정
- **데이터 로딩 전환(fetch)**: `python3 -m http.server`로 띄운 로컬 서버에서 정상 로드·매칭 확인. `file://`로 직접 열었을 때는 fetch가 차단되어 안내 에러 메시지가 뜨는 것까지 스크린샷으로 확인
- **celebs/ 페이지**: `celebs/index.html`에서 유명인 클릭 → 개별 페이지 이동 → "나와 닮은 사주 확인하기" 링크로 메인 복귀까지 실제 클릭 이동 확인, 161개 파일 생성 확인 (파일명 충돌 없음)

## ✅ 콘텐츠 검토 완료 (2026-08-04)
논란 소지가 있던 3명을 같은 일주의 대체 인물로 교체했습니다 (60갑자 커버리지는 그대로 유지):
- 조니 뎁 → **메릴 스트립** (癸未)
- 나폴레옹 → **니콜라 테슬라** (甲午)
- 잭 웰치 → **펠레** (己亥)

## ✅ 톤 검토 대상 제거 완료 (2026-08-04)
정치인 4명(버락 오바마, 빌 클린턴, 조지 W. 부시, 앙겔라 메르켈)과 이재용을 최종 제거했습니다.
모두 해당 갑자의 2번째 인물이었기 때문에 60갑자 커버리지는 그대로 유지되고, 甲子·乙丑·己巳·甲戌·辛巳 5개 갑자만 다시 1명(조수미/마돈나/윤여정/박지성/전지현)으로 돌아갔습니다.

## ✅ 모토/어록 검증 (2026-08-05, 제한적 범위)
161명 전원의 모토를 1차 자료까지 대조해 완전히 검증하는 건 현실적으로 어려워, 두 가지 방식으로 리스크를 낮췄습니다:
1. **명백한 오귀속 인용문 교체**: 인터넷에 흔히 도는 유명한 "가짜 인용문"으로 알려진 두 건을 발견해 교체
   - 에이브러햄 링컨: "나는 천천히 걷지만 절대 뒤로 걷지 않는다" (출처 불확실) → "국민의, 국민에 의한, 국민을 위한 정부" (게티즈버그 연설, 1863 — 검증된 실제 연설문)
   - 마크 트웨인: "20년 후 당신은..." (Quote Investigator 등에서 트웨인 저작으로 확인되지 않는다고 알려진 인터넷 밈성 인용문) → "책을 읽지 않는 사람은 글을 읽지 못하는 사람보다 나을 게 없다" (『푸든헤드 윌슨』 캘린더, 실제 출간 저작)
2. **구조적 안내 문구 추가**: 모든 모토 항목에 "실제 어록이거나 공개된 자료를 바탕으로 요약·재구성한 대표 문구"라는 안내를 결과 카드(`index.html`)·유명인 개별 페이지(`celebs/*.html`)·이용약관(`terms.html`) 세 곳에 명시해, 따옴표 표기가 검증된 원문 그대로임을 보증하지 않는다는 점을 이용자에게 투명하게 전달

⚠️ **한계**: 나머지 159명의 모토 중에도 유사한 오귀속 사례가 있을 가능성을 배제할 수 없습니다. 트래픽이 늘거나 논란 소지가 걱정되면 인물별로 추가 검증하는 것을 권장합니다.

## 왜 JSON으로 분리했나
- 유명인 DB가 늘어날수록(60갑자 커버리지 확장 등) 코드 수정 없이 데이터만 추가/수정 가능
- 콘텐츠 담당자가 코드를 몰라도 `celebrities.json`만 편집하면 되도록
- 추후 웹 프레임워크로 이식할 때 API가 이 JSON을 그대로 응답으로 내려줄 수도 있음

## 관련 노트
- [[사주 웹사이트 프로젝트 계획]]
- [[사주 웹사이트 - 유명인 일주·오행 태깅]]
- [[사주 웹사이트 - 매칭 로직 프로토타입]] (이전 버전, 로직 설명용)

## 다음 액션
- [x] ~~`celebrities.json`에 미커버 38개 갑자 인물 추가~~ → 완료, 60갑자 100% 커버
- [x] ~~프론트엔드(입력 폼 + 결과 카드 UI) 프로토타입 제작~~ → `index.html` 완료
- [x] ~~⚠️ 콘텐츠 검토 대상 3명 포함 여부 최종 결정~~ → 메릴 스트립/니콜라 테슬라/펠레로 교체 완료
- [x] ~~DB를 120~180명 규모로 확장~~ → 121명 달성 (갑자당 2~3명)
- [x] ~~정치인 4명 및 이재용 포함 여부 최종 결정~~ → 5명 모두 제거, 116명으로 확정
- [x] ~~결과 카드 이미지 저장/공유 기능~~ → Canvas API 기반으로 구현 완료 (다운로드 + Web Share API)
- [x] ~~150~180명까지 추가 확장~~ → 161명 달성, 전 갑자 최소 2명 확보
- [x] ~~개인정보처리방침 / 이용약관 페이지 작성~~ → `privacy-policy.html`, `terms.html` 완료 (연락처/시행일 등 대괄호 표시 항목은 실제 정보로 채워야 함)
- [x] ~~JSON ↔ index.html 데이터 중복 제거~~ → `fetch()`로 전환 완료 (로컬 서버 필요, file:// 열면 안내 메시지 표시)
- [x] ~~Open Graph 메타태그 추가~~ → og-image.png 생성 + og/twitter 메타태그 삽입 완료
- [x] ~~유명인별 개별 URL 구조 설계 (SEO 대비)~~ → `celebs/` 폴더에 161개 개별 페이지 + 목록 페이지 + sitemap.xml 생성 완료
- [x] ~~모토/어록 출처 재검증~~ → 완료(제한적 범위). 링컨·마크 트웨인의 흔한 오귀속 인용문을 검증된 실제 어록으로 교체, 모토 항목 전체에 "실제 어록이거나 재구성된 문구"라는 안내를 결과 카드·유명인 페이지·이용약관 3곳에 추가해 구조적으로 리스크를 낮춤. **161명 전원의 1차 자료 대조까지는 하지 않았음** — 필요시 추가 검증 권장
- [x] ~~반응형 디자인 최종 점검 (PC 화면)~~ → 1440×900 데스크톱에서 메인/결과/카드모달/유명인목록/개인정보방침 페이지 모두 확인, 레이아웃 깨짐 없음
- [x] ~~배포용 저장소 분리 + 도메인/호스팅 연결~~ → GitHub + Vercel 배포, `sajutwin.com` 도메인 DNS 연결 완료
- [ ] 실제 도메인 배포 전 디자인 시스템(색상/폰트) 확정 및 반영
- [ ] 카드 이미지에 실제 웹폰트 임베드 고려 (현재는 시스템 폰트 의존이라 OS별로 미묘하게 다르게 렌더링될 수 있음)
- [x] ~~sitemap.xml / og:url의 `YOUR-DOMAIN.example`을 실제 도메인으로 교체~~ → `sajutwin.com`으로 교체 완료
- [x] ~~privacy-policy.html / terms.html의 연락처 이메일 교체~~ → `goldgarden9620419@gmail.com` 반영 완료 (시행일은 정식 오픈일 확정 후 교체 예정)
