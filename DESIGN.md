# DESIGN.md — 디자인 기준 파일

이 파일은 이 프로젝트에서 화면(랜딩 페이지 등)을 만들 때 클로드가 반드시 따라야 하는 기준입니다.
`( )` 안은 아직 정하지 않은 값입니다. 취향이 생기면 직접 고쳐 주세요.

## 1. 목표

- 랜딩 페이지를 "AI가 만든 티"가 나지 않게, 사람이 공들여 만든 것처럼 만든다.
- 기술 스택: HTML과 CSS만 사용 (프레임워크·빌드 도구 없이 파일 하나로 열리게).
- 스마트폰에서 먼저 잘 보여야 하고, PC에서도 깨지지 않아야 한다.

## 2. 하지 말 것 (AI 티 나는 패턴 금지)

- 보라색→파란색 그라데이션 배경, 이모지 남발, 카드 안에 카드 안에 카드 구조 금지.
- 모든 섹션이 "가운데 정렬 제목 + 3열 카드" 반복인 뻔한 레이아웃 금지.
- 뜻 없는 문구("혁신적인 솔루션", "당신의 비즈니스를 성장시키세요") 금지. 구체적인 문장만 쓴다.
- 그림자·둥근 모서리·애니메이션을 습관처럼 넣지 않는다. 이유가 있을 때만.

## 3. 색

- 기본은 **바탕 1색 + 글자 1색 + 포인트 1색**, 총 3색 이내로 시작한다.
- 바탕: ( 아직 미정 — 예: 따뜻한 흰색 #FAF9F6 )
- 글자: ( 아직 미정 — 예: 진한 먹색 #1A1A1A )
- 포인트: ( 아직 미정 — 버튼·링크 등 강조 한 곳에만 사용 )

## 4. 글꼴과 글자

- 한글 본문: ( 아직 미정 — 예: Pretendard, 시스템 기본 글꼴 순서로 대체 )
- 본문 크기 16px 이상, 줄 간격 1.6 전후. 한 줄이 너무 길어지지 않게 본문 폭을 제한한다.
- 제목은 크기 차이로만 구분하고, 색·기울임·밑줄을 섞어 쓰지 않는다.

## 5. 여백과 배치

- 섹션 사이는 넉넉하게, 요소 사이 간격은 일정한 배수(예: 8px 단위)로 통일한다.
- 첫 화면(히어로)은 비우는 것을 두려워하지 않는다: 제목 + 한 문장 + 버튼 하나면 충분.
- 작은 노트북 화면에서도 첫 화면의 핵심이 스크롤 없이 보여야 한다.

## 6. 작업 순서 (설치된 도구 활용)

1. 이 파일(DESIGN.md)을 먼저 읽고 기준을 확인한다.
2. `image-to-code`로 시안 이미지를 먼저 만들어 방향을 확인받는다.
3. `design-taste-frontend` 기준으로 코드를 작성한다.
4. `agent-browser`로 실제 화면을 열어 스크린샷으로 확인한다.
5. `web-design-guidelines`로 접근성·사용성을 검수하고 고친다.

## 7. 완성 판정 기준

- 스마트폰 폭(375px)과 PC 폭(1280px)에서 모두 깨지지 않는다.
- 위 2번의 금지 패턴이 하나도 없다.
- 처음 보는 사람이 3초 안에 "무엇을 하는 페이지인지" 말할 수 있다.
