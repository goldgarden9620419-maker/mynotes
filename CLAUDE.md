# CLAUDE.md

이 저장소는 사용자의 Obsidian vault(`mynotes`)입니다.

## 노트 작성 시 자동 저장 규칙

사용자가 노트/메모/정리/요약 등을 만들어달라고 요청하면:

1. 적절한 폴더(대개 `00-Inbox/`, 분류가 명확하면 `01-Projects/`, `02-Areas/`, `03-Resources/`)에 `.md` 파일로 작성한다.
2. **작성 후 별도로 확인받지 않고 즉시 git add + commit + push까지 완료한다.** 이 저장소는 개인 노트 vault이며, 사용자가 이미 "요청하면 자동으로 저장해달라"고 명시적으로 지시했다.
3. 커밋 메시지는 노트 내용을 짧게 요약해서 작성한다.
4. push가 끝나면 사용자에게 무엇을 만들어 어디에 저장했는지 한두 문장으로 알려준다.

## 참고

- 이 저장소를 Obsidian 앱에서 여는 사용자는 Git 플러그인의 자동 pull(10분 간격 + 시작 시 pull)이 설정되어 있으므로, 여기서 push한 노트는 자동으로 로컬 Obsidian에도 반영된다.
- 파일명/노트 제목은 검색하기 쉽게 명확하게 짓는다.
